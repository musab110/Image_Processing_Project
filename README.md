# Image_Processing_Project
<div dir="rtl" style="text-align: right;">

# 🇹🇭 مشروع OCR للغة التايلاندية: من البيانات إلى النموذج الخبير 🇹🇭

---

**إعداد الطلاب:** مصعب الجعشني - سليمان الجبرتي  
**إشراف الدكتور:** أكرم الصباري

---

## مقدمة

يهدف هذا المشروع إلى تطوير نظام التعرف البصري على الحروف (OCR) للغة التايلاندية باستخدام نماذج YOLOv8 للكشف عن النصوص ومكتبة EasyOCR لقراءة النصوص. يمر المشروع بعدة مراحل تبدأ بالإعدادات الأولية، ثم اختبار نموذج أولي، وصولاً إلى تدريب "نموذج خبير" (Expert Model) قادر على التعامل مع النصوص التايلاندية في الصور المعقدة من العالم الحقيقي. يكتمل المشروع بواجهات تفاعلية مبنية باستخدام مكتبة Gradio لسهولة الاختبار والتقييم.

---

## خطوات التشغيل في Google Colab

**ملاحظة هامة:** عند تشغيل دفتر الملاحظات في Google Colab، يجب تنفيذ الخلايا بالترتيب. الخلايا التي تحمل علامة "(يجب تشغيلها عند العودة)" تعني أنها خطوات أساسية يجب تكرارها عند بدء جلسة Colab جديدة أو استئناف العمل بعد انقطاع الاتصال.

### **المرحلة 1: الإعدادات الأولية وتفعيل الـ GPU**

<p style="font-size: 16px;"><b>(يجب تشغيلها عند العودة)</b></p>

*   **الغرض:** في هذه المرحلة، يتم التحقق من أن بيئة Google Colab تستخدم وحدة معالجة الرسوميات (GPU). استخدام الـ GPU ضروري لتسريع العمليات الحسابية المكثفة التي تحتاجها نماذج التعلم العميق لتدريب وتشغيل النماذج بكفاءة.
*   **التعليمات:**
    1.  شغّل الخلية المخصصة لهذه المرحلة.
    2.  تأكد من أن المخرجات تظهر تفاصيل حول وحدة GPU المخصصة لك، مما يؤكد أنها تعمل بشكل صحيح.

---

### **المرحلة 2: تجهيز بيئة العمل (Setup)**

<p style="font-size: 16px;"><b>(يجب تشغيلها عند العودة)</b></p>

*   **الغرض:** تتضمن هذه المرحلة خطوتين أساسيتين: أولاً، الاتصال بـ Google Drive الخاص بك لتمكين دفتر الملاحظات من الوصول إلى ملفات المشروع وحفظ المخرجات فيه. ثانيًا، تثبيت جميع مكتبات Python اللازمة لتشغيل المشروع، مثل `ultralytics` (لنماذج YOLOv8)، `easyocr` (لقراءة النصوص)، و`gradio` (لبناء الواجهات التفاعلية).
*   **التعليمات:**
    1.  شغّل الخلية المخصصة لهذه المرحلة.
    2.  سيُطلب منك المصادقة والسماح لـ Google Colab بالوصول إلى Google Drive الخاص بك. يجب إتمام هذه الخطوة بنجاح.
    3.  انتظر حتى تكتمل عملية تثبيت المكتبات.

---

### **المرحلة 3: اختبار النموذج الأصلي (v1)**

<p style="font-size: 16px;"><b>(يجب تشغيلها عند العودة)</b></p>

*   **الغرض:** في هذه المرحلة، يتم تحميل النموذج الأولي للكشف عن النصوص (YOLOv8) الذي تم تدريبه مسبقًا على مجموعة بيانات أولية (يُشار إليه بالاسم `best.pt`). الهدف هو تقييم الأداء الأساسي لهذا النموذج على صور تايلاندية واقعية وتحديد جوانب الضعف التي تحتاج إلى تحسين.
*   **التعليمات:**
    1.  **قبل التشغيل:** تأكد من أن مسار تحميل النموذج داخل الخلية يشير بشكل صحيح إلى ملف نموذجك `best.pt` في Google Drive.
    2.  شغّل الخلية المخصصة لهذه المرحلة.
    3.  بعد تحميل النموذج وقارئ النصوص (EasyOCR)، سيتم إطلاق واجهة Gradio تفاعلية.
*   **كيفية الاستخدام:** يمكنك استخدام واجهة Gradio لرفع صور تحتوي على نص تايلاندي ومشاهدة كيف يقوم النموذج v1 بالكشف عن مناطق النص.

---

### **المرحلة 4: التدريب المتقدم (v2) - بناء النموذج الخبير**

<p style="font-size: 16px;"><b>(هذه الخلية تقوم بالتدريب، لا تحتاج لتشغيلها في كل مرة، فقط عند الحاجة لإعادة التدريب أو بعد تعديل البيانات)</b></p>

*   **الغرض:** هذه هي مرحلة التدريب الفعلي والمتقدم للمشروع. بناءً على الملاحظات والنتائج من المرحلة 3، يتم هنا تطبيق عملية "الضبط الدقيق" (Fine-tuning) على النموذج. يبدأ التدريب من النموذج v1 ويستمر باستخدام "حزمة بيانات خارقة" (Super-Dataset) شاملة تحتوي على أنواع مختلفة من الصور (بما في ذلك الصور الأصلية والسلبية والاحترافية). هذا يهدف إلى تعليم النموذج كيفية التعامل بفعالية مع تعقيدات وتنوع النصوص في العالم الحقيقي.
*   **التعليمات:**
    1.  **قبل التشغيل:** تأكد من أن مسار ملف الـ ZIP الذي يحتوي على حزمة البيانات الخارقة في Google Drive محدد بشكل صحيح داخل الخلية.
    2.  شغّل الخلية المخصصة لهذه المرحلة.
    3.  ستقوم الخلية بفك ضغط البيانات، إعداد ملفات التكوين اللازمة (مثل `data.yaml`)، ثم بدء عملية التدريب باستخدام إطار عمل YOLOv8 لعدد محدد من الدورات (Epochs).
*   **ملاحظة:** قد تستغرق عملية التدريب وقتًا طويلاً حسب موارد الـ GPU المتاحة وحجم مجموعة البيانات.

---

### **المرحلة 5: العرض النهائي للنموذج الخبير (v2)**

<p style="font-size: 16px;"><b>(يجب تشغيلها عند العودة، بعد تشغيل المرحلتين 1 و 2 بنجاح وبعد اكتمال التدريب في المرحلة 4)</b></p>

*   **الغرض:** بعد اكتمال التدريب في المرحلة 4 ونجاح بناء "النموذج الخبير"، يتم في هذه المرحلة حفظ هذا النموذج في Google Drive، ثم تحميله. بعد ذلك، يتم إطلاق واجهة Gradio تفاعلية احترافية ومُحسّنة. تُستخدم هذه الواجهة لاختبار الأداء النهائي للنموذج الخبير في كشف وقراءة النصوص التايلاندية، مما يسمح بمقارنة الأداء وتحليل التحسينات التي تمت.
*   **التعليمات:**
    1.  **قبل التشغيل:** تحقق من مسار تحميل وحفظ النموذج الخبير في الخلية، وتأكد من أنه يشير إلى النموذج المدرب الأخير الذي تم إنشاؤه في المرحلة 4 (وقد تحتاج لتعديل اسم المجلد الناتج من YOLO، مثل `train/weights/best.pt`).
    2.  شغّل الخلية المخصصة لهذه المرحلة.
    3.  سيتم حفظ النموذج، ثم تحميله، ثم إطلاق الواجهة التفاعلية.
*   **كيفية الاستخدام:** ارفع صورًا تايلاندية، خاصة تلك التي كانت تمثل تحديًا للنموذج الأولي v1، وشاهد كيف يكشف النموذج الخبير النص ويستخرجه بدقة.

---

## ملاحظات هامة واستكشاف الأخطاء وإصلاحها

*   **الاتصال بـ Google Drive:** يجب التأكد دائمًا من إعادة تشغيل خلايا الاتصال بـ Google Drive (المرحلة 2) إذا انقطع اتصال جلسة Colab لأي سبب.
*   **مسارات الملفات:** جميع المسارات المشار إليها في دفتر الملاحظات (مثل `/content/drive/MyDrive/yolo_ocr_project/...`) هي مسارات داخل Google Drive الخاص بك. من الضروري التأكد من أن جميع ملفات البيانات والنماذج موجودة في المواقع المحددة ضمن مجلد `yolo_ocr_project` في Drive الخاص بك.
*   **النماذج المدربة:** إذا قمت بتغيير اسم النموذج الذي تحفظه أو مسار مجلد التدريب الذي تنشئه مكتبة YOLO (مثل `train1`, `train2`, وهكذا)، فتأكد من تحديث المسارات المقابلة في جميع الخلايا التي تقوم بتحميل أو حفظ النماذج.
*   **موارد GPU:** مهام تدريب النماذج الكبيرة وتوليدها يمكن أن تكون مكثفة على موارد الـ GPU. إذا واجهت مشكلات تتعلق بذاكرة الـ GPU أو بطء شديد في الأداء، قد يكون من الضروري التفكير في استخدام اشتراكات Google Colab Pro أو Pro+ للحصول على موارد حوسبة أقوى.
*   **تنزيل البيانات والنماذج:** إذا قمت بتشغيل هذا المشروع على جهاز كمبيوتر مختلف، أو في بيئة Colab جديدة تمامًا، فستحتاج إلى التأكد من أن جميع ملفات البيانات والنماذج التي تعتمد عليها المراحل المختلفة موجودة في Google Drive الخاص بهذا الحساب الجديد قبل بدء التشغيل.

</div>
